sap.ui.define([
	"sap/ui/core/mvc/Controller"
], function(Controller) {
	"use strict";

	return Controller.extend("POReport.controller.POHeader", {
		
		GoToPOItem: function(oEvent) {
			//var oContext = oEvent.getSource().getBindingContext();
			//console.log(oContext.getPath());
			//alert(oContext.getProperty("LifecycleStatusDescription"));
		}
		
	});
});